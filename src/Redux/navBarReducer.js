let initialState = {
    friends:[
        {id:1, name:"Alex"},
        {id:2, name:"Asel"},
        {id:3, name:"Samat"}
    ]
}

const navBarReducer = (state = initialState, action) => {

    return state
}

export default navBarReducer()